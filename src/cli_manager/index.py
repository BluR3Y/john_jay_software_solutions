import argparse
import os
import warnings
warnings.simplefilter(action='ignore', category=FutureWarning)

from dotenv import load_dotenv
from logging_manager import get_logger

from .utils import single_select_input

def main():
    pass

if __name__ == "__main__":
    main()